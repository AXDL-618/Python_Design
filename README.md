# Python_Design
compare.py ：图像相似度对比模块<br>
draw.py ：画图板模块<br>
main.py ：主程序

获取项目所需对应的包，可通过以下指令一键配置安装

```cmd
pip install -r requirements.txt
```

运行main.py主程序，程序会自动调用相似度对比与画图板模块，实现完整画图板相似度比对程序。

所有预设图像存在于db目录下，分为easy与hard，可自由添加图片。
